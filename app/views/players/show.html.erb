<% content_for(:title, "#{@player.first_name}") %>

<h1><%= @player.first_name %></h1>

<table>
	<thead>
		<tr>
			<td>First Name</td>
			<td>Surname</td>
		</tr>
	</thead>

	<tbody>
		<tr>
			<td><%= @player.first_name %></td>
			<td><%= @player.surname %></td>
		</tr>
	</tbody>
</table>

<%= link_to "Home Page", root_path %>